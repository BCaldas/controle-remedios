<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.brunoedalcilene.horadoremdio">

    <application
        android:allowBackup="true"
        android:icon="@drawable/principal"
        android:label="@string/app_name"
        android:roundIcon="@drawable/principal"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity
            android:name=".RemediosActivity"
            android:label="@string/title_activity_remedios"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CadastroRemedioActivity"
            android:label="@string/title_activity_cadastro_remedios"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PacientesActivity"
            android:label="@string/title_activity_pacientes"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CadastroPacienteActivity"
            android:label="@string/title_activity_cadastro_paciente"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PrincipalActivity"
            android:label="@string/title_activity_principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>